# eigenda-rs
EigenDA Rust Tooling

Run

```bash
git submodule update --init --recursive
cd contract-bindings/crates/contract-bindings/eigenda/contracts
yarn install
forge install
yarn run build
cd ../..
cargo build
```
